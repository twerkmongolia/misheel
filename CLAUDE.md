@AGENTS.md

# Twerk Mongolia

Улаанбаатар дахь бүжгийн студийн платформ: танилцуулга сайт, анги/курсын
бүртгэл, онлайн дэлгүүр, төлбөр, админ удирдлага. Хоёр хэл (mn анхдагч, en).

**Next.js 16 (App Router, Turbopack) · React 19 · Tailwind v4 · Supabase · Bonum Gateway**

## Тушаалууд

```bash
npm run dev          # localhost:3000
npm run dev:2        # localhost:3002 — хоёр дахь сервер (өөр cookie сав)
npm run check        # tsc --noEmit && eslint && vitest run — commit-ын өмнө
npm run check:build  # .next-check рүү build — dev сервер унтраахгүй
```

⚠️ `next dev` нь нэг хавтсанд хоёр серверийг хаадаг — `NEXT_DIST_DIR` өөр
өгвөл зөвшөөрнө (`dev:2` яг үүнийг хийдэг). Хоёр порт = хоёр өөр origin =
тусдаа cookie: хоёр хэрэглэгчээр зэрэг нэвтрэх цорын ганц арга.

`npm test` (vitest) нь зөвхөн ЦЭВЭР функцуудыг хамарна — checksum, i18n,
format. Хуудас, action, RLS-д нэгжийн тест байхгүй: тэдгээрийн зөв эсэхийг
дев сервер рүү бодит хүсэлт илгээж, Supabase дээрх үр дүнг нь харж шалгана.

## Бүтэц

```
src/app/
  layout.tsx          үндэс: <html>, фонт, CSP nonce, Reveal ажиглагч
  [locale]/layout.tsx ЭРХЭЭР салдаг: сурагч → StudentShell, бусад → сайтын чимэглэл
    (marketing) (shop) (booking) (auth) (account)
  admin/              хэлгүй, өөрийн AdminShell
  auth/               OAuth эхлэл ба буцах цэг (хэлгүй)
  api/payments/webhook
src/proxy.ts          middleware: хэл, CSP nonce, session, /admin хамгаалалт
src/actions/          Server Actions — бүх бичилт эндүүр
src/lib/data.ts       уншилтууд (RLS-тэй хэрэглэгчийн client)
supabase/migrations/  дугаарласан SQL — ГАРААР Supabase SQL Editor дээр ажиллуулна
```

## Дүрмүүд

**Бизнес логик Postgres дотор.** `place_order`, `enroll_course`,
`settle_payment` зэрэг нь `security definer` функцууд: дүн бодох, нөөц хасах,
төлөв солих нь НЭГ транзакц, НЭГ түгжээний дор болно. TypeScript дээр
задалж бичвэл зэрэг ирсэн хоёр хүсэлт хоёулаа «боломжтой» гэж уншина.

**RLS бол жинхэнэ хамгаалалт**, код биш. Хуудас, action бүр
`lib/supabase/server.ts` -ийн хэрэглэгчийн client-ээр явна.
`createAdminClient()` (service-role) нь ЗӨВХӨН webhook зэрэг session
байхгүй газарт — тэнд итгэлийн хил нь гарын үсэг дээр байдаг.

**Толь бичиг:** `mn` бол эх сурвалж, `Dictionary = typeof mn` тул `en` нь
яг ижил түлхүүртэй байх ёстой (tsc барина). Зарим түлхүүр ТООЦООЛСОН
нэрээр уншигддаг (`t.orderStatus[status]`, `t.common[key]`) — «хэрэглэгдээгүй»
гэж харагдах ч устгаж болохгүй.

**Тайлбар нь ЯАГААД-ыг бичнэ**, юу болохыг биш. Монголоор. Шийдвэрийн
шалтгаан, татгалзсан хувилбар, урхи — дараагийн хүн ижил алдаа давтахгүйн тулд.

## Урхинууд (өмнө нь хохирол учруулсан)

**`[data-rv]` нь CSS-ээр НУУГДАНА.** `html.rv-on [data-rv]:not(.is-in)` нь
`opacity: 0`. Нээхийг зөвхөн `Reveal` ажиглагч хийнэ — тиймээс тэр нь
**үндсэн layout** дээр сууна. Аль нэг салбар түүнийг алгасвал тэр салбарын
БҮХ агуулга үл үзэгдэх болно (нэвтэрсэн сурагчид яг ингэж болсон).

**Server Action доторх `redirect()` нь хуудсыг дахин АЧААЛДАГГҮЙ** — client
шилжилт болно. `<dialog>` нь DOM-д үлдэх тул өөрөө хаагдахгүй: `FormDialog`,
`CardDialog` хоёр хаяг солигдоход хаадаг. Үүнгүйгээр ажилтан «болсонгүй» гэж
бодоод дахин дарж, есөн ижил бараа үүсгэсэн.

**Зураг:** Server Action-ы бие 5MB (`next.config.ts`), нэг зураг 4MB
(`lib/uploads.ts`). Хоёр тоо ТЭНЦҮҮ байж болохгүй — multipart-ын нэмэлт
байтууд халина. Хөтөч дээр `shrink-image.ts` урьдчилан шахна.

**Storage файл нь DB-г дагаж устдаггүй.** Мөр устгахдаа
`storage.from('media').remove([...])` -ийг гараар дуудна.

**Сагс бол cookie.** Дотор нь устсан/идэвхгүй бараа үлдэж болзошгүй —
`placeOrder` нь захиалга үүсгэхийн ӨМНӨ бодит байдалтай тулгаж цэвэрлэдэг.

## Төлбөр

`PAYMENT_PROVIDER=bonum` (эсвэл `mock`). Урсгал:

```
бараа/анги → place_order | enroll_course  (захиалга + payments мөр)
           → createInvoice → Bonum руу шилжинэ → төлнө
           → webhook → x-checksum-v2 шалгана → settle_payment
           → захиалга paid → trigger → элсэлт active → Telegram нээгдэнэ
```

Дэлгүүрт САГС БАЙХГҮЙ: бараан дээрээс шууд `?variant=…&qty=…` гэж худалдан
авалтын хуудас руу орно. Сагс нь cookie дотор хуучирсан мөр хадгалж,
нэмэлт гурван алхам үүсгэдэг байв.

`settle_payment` нь идэмпотент (давтан webhook юу ч хийхгүй), дүнг тулгадаг
(зөрвөл юу ч хийхгүй, логт хашгирна). Дэлгэрэнгүй:
`src/lib/payments/README.md`.

⚠️ Bonum-ын гурван урхи, гурвуулаа ЧИМЭЭГҮЙ унагадаг байсан:
`callback` нь ХЭРЭГЛЭГЧИЙГ буцаах хаяг (webhook БИШ — түүнийг merchant
тохиргоон дээр нэг удаа бүртгүүлнэ); `expiresIn` нь ЗААВАЛ илгээх талбар;
токеныг нэг мөчид нэг л удаа авч болно тул `payment_tokens` хүснэгтээр
хуваалцана.

**Элсэгчийн тоо = ТӨЛСӨН хүн.** `courses.enrolled_count` нь зөвхөн
`active`/`completed` -ийг тоолно. Суудлын шалгалт нь түүн дээр нэмж
саяхны `pending_payment` -ийг тооцдог (30 минутын барьц) — тэгэхгүй бол
10 суудалтай ангид 40 хүн төлчихнө (§ migration `paid_seats`).

## Өгөгдлийн сан

Миграцууд нь ГАРААР ажиллуулагддаг (Supabase CLI холбогдоогүй). Шинэ
миграц бичээд хэрэглэгчид өгч, ажиллуулсны дараа REST API-аар шалгана.
Хэрэглэсэн миграцийг ЗАСАХГҮЙ — шинийг нэмнэ.
